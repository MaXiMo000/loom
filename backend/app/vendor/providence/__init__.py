"""Vendored from github.com/MaXiMo000/providence (MIT, same author) at
commit 56c992a5b62c0b613c33ad50b07b65cc35e7ed04 — `check.py` + `spec.py`
only, unmodified except this note. providence-evidence isn't published to
PyPI yet (SPEC.md §7.2's stated fallback: "vendor the ~200 lines of
check.py directly, same receipt/invariant precedent of small stdlib-only
deps"). Swap this for a real `providence-evidence` dependency once it's
published — don't let this copy silently drift from the real package."""
