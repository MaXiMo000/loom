# loom

Not built yet — this is the planning stage of a new project.

**Read [`SPEC.md`](SPEC.md)** for the full architecture: what this is, why,
the scope decisions and their reasoning, the data model, the API, the 3D
rendering approach, and the phased build order.

**Read [`KICKOFF_PROMPT.md`](KICKOFF_PROMPT.md)** if you're an agent about
to start building this — paste it into a fresh session.

This README gets rewritten once there's a real thing to describe (see
SPEC.md §9's own note on not front-loading marketing copy before the
project exists).
